import React from 'react';
import ReactDOM from 'react-dom/client';
import AuthorVoiceApp from './components/AuthorVoiceApp';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AuthorVoiceApp />
  </React.StrictMode>
);
